import {Button} from "@/common/components/ui/button.tsx";

export const Login = () => (
    <div>
      <a href="/" className="mr-2 text-black">Log in</a>
      <Button variant="default">Start Yor Free Trial</Button>
    </div>
  )